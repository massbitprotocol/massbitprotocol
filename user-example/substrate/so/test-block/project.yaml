schema:
  file: ./schema.graphql

dataSources:
  - kind: substrate
    name: substrate-block
    mapping:
      language: rust
      handlers:
        - handler: handleBlock
          kind: substrate/BlockHandler
